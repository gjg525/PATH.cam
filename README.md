# PATH.cam

This package runs the PATH (population abundance in targeted habitats) model on agent-based simulations for population abundance estimation. The PATH model is an unmarked camera trap abundance estimator that allow for targeted (non-random) sampling designs.
